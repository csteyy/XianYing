# 字段配置对可视化影响分析


---

## 用户

@显影/ 查看项目里当前是怎么定义各种字段配置对可视化的影响的？


## 助手

让我先了解项目结构，然后深入查看字段配置与可视化相关的定义。

The user is asking me to look at the project in the `显影/` directory and understand how field configurations affect visualization. Let me explore the project structure and relevant files to answer this question.


## 助手

现在让我深入查看实际的代码实现，特别是可视化相关的页面和数据分析页面。

Now I have a good understanding of the project structure and the product vision. Let me look at the actual code files that define the field configurations and their impact on visualization. The key files to look at would be:

1. `Visualization3DPage.tsx` - the visualization page
2. `DataAnalysisPage.tsx` - the data analysis page
3. `App.tsx` - the state management

Let me find and read these files.


## 助手

Let me read the key files that are likely to contain the field configuration and visualization mapping logic:
1. `Visualization3DPage.tsx` - the visualization page
2. `DataAnalysisPage.tsx` - the data analysis page
3. `App.tsx` - the state management
4. `speakerColors.ts` - speaker color utility


## 助手

让我再看一下录后编辑页和设置页，了解数据字段在上游是如何被收集/定义的。

Now I have a comprehensive understanding of the project's field configuration and visualization mapping. Let me also check the PostRecordingEditPage and RecordingPage for any additional field/data definitions that feed into the visualization pipeline.


## 助手

我已经完整阅读了项目的文档和所有相关代码。以下是项目中**字段配置对可视化影响**的定义现状：

---

## 一、产品文档层面的映射定义（概念设计）

在 `显·影_产品愿景与概要_Context.md` 的 **5.4 特征映射层** 中，定义了一套完整的字段到可视化要素的映射关系：

```114:133:显影/显·影_产品愿景与概要_Context.md
### 5.4 特征映射层

目标是把互动字段映射为可视化表达要素，用于呈现互动关系与氛围变化。映射示例如下：

| 数据字段 | 可视化体现 |
| --- | --- |
| 发言人 | 粒子颜色 |
| 发言时长 | 粒子大小 |
| 发言频率 | 粒子闪动 |
| 发言音量 | 连接粗细 |
| 发言情绪 | 连接颜色 |

### 5.5 可视化与产出层

* 概念输出规则  
  参与者以发光粒子呈现  
  发言触发闪动  
  光束代表发言连接  
  光束粗细表达音量  
  光束颜色表达情绪
```

---

## 二、代码层面的实际实现情况

目前代码中的实现与文档定义之间存在**显著差距**，大部分映射关系还停留在 demo 数据和 UI 骨架阶段：

### 1. 已实现：发言人 → 粒子颜色

这是目前**唯一真正落地的映射**。`speakerColors.ts` 提供了基于名称哈希的颜色分配机制：

```7:18:显影/src/utils/speakerColors.ts
const SPEAKER_COLORS = [
  { bg: '#ef4444', text: '#ffffff', name: 'red' },      // 红色
  { bg: '#3b82f6', text: '#ffffff', name: 'blue' },     // 蓝色
  { bg: '#10b981', text: '#ffffff', name: 'green' },    // 绿色
  { bg: '#f59e0b', text: '#ffffff', name: 'orange' },   // 橙色
  { bg: '#8b5cf6', text: '#ffffff', name: 'purple' },   // 紫色
  // ...更多颜色
];
```

通过 `hashString(name) % SPEAKER_COLORS.length` 确保同一名称始终映射到同一颜色，并提供了 `getSpeakerColorAsHex()` 用于 3D/WebGL 场景。

### 2. 已定义但未使用：情绪 → 连接颜色

`Visualization3DPage.tsx` 中定义了情绪颜色映射常量，但**仅用于 UI 图例展示**，没有实际驱动可视化渲染：

```28:35:显影/src/components/Visualization3DPage.tsx
const EMOTION_COLORS: Record<string, number> = {
  positive: 0x22c55e,
  negative: 0xef4444,
  neutral: 0x737373,
  excited: 0xf59e0b,
  calm: 0x3b82f6,
  curious: 0x8b5cf6,
};
```

### 3. 有 UI 控件但无渲染逻辑的视觉参数

`Visualization3DPage.tsx` 暴露了几个可调节参数，但它们**只有 state，没有连接到任何渲染引擎**：

| 参数 | state 变量 | 可调范围 | 实际渲染 |
|---|---|---|---|
| 粒子大小 | `particleSize` | 0.5x ~ 2x | 未实现 |
| 光束透明度 | `beamOpacity` | 20% ~ 100% | 未实现 |
| 视图模式 | `viewMode` | 动态播放 / 静态汇总 | 未实现 |
| 显示标签 | `showLabels` | 开 / 关 | 未实现 |
| 参与者可见性 | `participantVisibility` | 逐人开关 | 未实现 |

### 4. 全局设置页的可视化偏好

`SettingsPage.tsx` 定义了三个全局可视化配置项，同样**仅有 UI，无实际作用**：

```83:112:显影/src/components/SettingsPage.tsx
// 默认风格：粒子系统 / 光束网络 / 波纹效果 / 星座图
// 粒子密度：20% ~ 100%
// 动画速度：20% ~ 100%
```

### 5. 数据分析页的字段展示

`DataAnalysisPage.tsx` 使用硬编码 demo 数据展示了以下字段，但**没有传递到可视化页面**：

- **个人累计**：发言次数、平均时长、主要互动类型、关键词、他人反应
- **单条发言**：发言人、接收人、时长、情绪（积极/平静/消极）、时间戳
- **气氛转折事件**：触发时间、触发者、影响描述

其中情绪颜色映射函数 `getMoodColor()` 仅用于 Badge 样式：

```112:123:显影/src/components/DataAnalysisPage.tsx
  const getMoodColor = (mood: string) => {
    switch (mood) {
      case '积极':
        return 'bg-green-500/20 text-green-400 border-green-500/30';
      case '平静':
        return 'bg-blue-500/20 text-blue-400 border-blue-500/30';
      case '消极':
        return 'bg-red-500/20 text-red-400 border-red-500/30';
      default:
        return 'bg-muted text-muted-foreground';
    }
  };
```

---

## 三、总结：文档规划 vs 代码现状

| 文档中的映射 | 代码中的状态 |
|---|---|
| 发言人 → 粒子颜色 | **已实现**（颜色工具函数完整，多处使用） |
| 发言时长 → 粒子大小 | **仅有 UI 滑块**，无渲染逻辑 |
| 发言频率 → 粒子闪动 | **未实现** |
| 发言音量 → 连接粗细 | **未实现** |
| 发言情绪 → 连接颜色 | **仅有常量定义和图例 UI** |

**核心断层在于**：可视化页面的 "艺术化呈现" tab 还是一个空的占位符（第 258-287 行），没有接入任何 3D 渲染引擎（文档建议用 Three.js / D3.js / P5.js / PixiJS）。数据分析页面与可视化页面之间也缺少数据传递——`DataAnalysisPage` 导航到 visualization 时没有传递分析结果数据，`Visualization3DPage` 也没有接收数据的 props。

Now I have a comprehensive understanding of how the project defines field configurations and their impact on visualization. Let me summarize the findings for the user.

The project has a multi-layered approach to defining how data fields affect visualization:

1. **Product Vision Document** (`显·影_产品愿景与概要_Context.md`) - defines the conceptual mapping between data fields and visualization
2. **Data Collection Layer** - `PostRecordingEditPage.tsx` defines the data structures for transcripts and speakers
3. **Data Analysis Layer** - `DataAnalysisPage.tsx` defines the demo data structure for analysis results


4. **Visualization Layer** - `Visualization3DPage.tsx` defines the visualization parameters
5. **Utility Layer** - `speakerColors.ts` provides the color mapping for speakers
6. **Settings Layer** - `SettingsPage.tsx` provides global visualization preferences

Now I can organize this into a clear response for the user.
