- **显影/**：React + Vite + Capacitor 项目，包含 Web 与 iOS 应用。  
  - 运行 Web：`cd 显影 && npm i && npm run dev`  
  - 构建并同步 iOS：`cd 显影 && npm run build && npx cap sync ios`，使用 Xcode 打开 `显影/ios/App/App.xcodeproj` 运行。

- **Flow_显·影.zip**：显·影前端代码的原始压缩包备份。
